'use strict';

function favIceCream(str) {
  return "I love " + str;
}

function shouting(str) {
  return str.toUpperCase();
}

function roundDown(dec) {
  return Math.floor(dec);
}

function theTruth() {
  return true;
}